/** DI token for the namespace repository port. */
export const NAMESPACE_REPOSITORY = Symbol('NAMESPACE_REPOSITORY');

/** DI token for the resolved application configuration. */
export const APP_CONFIG = Symbol('APP_CONFIG');
