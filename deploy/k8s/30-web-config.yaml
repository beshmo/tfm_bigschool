apiVersion: v1
kind: ConfigMap
metadata:
  name: okvns-web-config
  namespace: okvns
  labels:
    app.kubernetes.io/name: okvns-admin-web
    app.kubernetes.io/part-of: okvns
data:
  # Reachable from the user's browser; update to the ingress host in real setups.
  OKVNS_API_BASE_URL: 'http://okvns.example.com/api'
